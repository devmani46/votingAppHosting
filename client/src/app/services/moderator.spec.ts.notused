import { TestBed } from '@angular/core/testing';

import { Moderator } from './moderator';

describe('Moderator', () => {
  let service: Moderator;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Moderator);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
